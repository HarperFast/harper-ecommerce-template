# Stub package for the integration test fixture.
# app.test.ts copies ../../server-timing/extension.mjs here before Harper
# boots so the real production extension is exercised over live HTTP.
extensionModule: ./extension.mjs
